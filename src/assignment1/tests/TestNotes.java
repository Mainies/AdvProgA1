package assignment1.tests;
/*
import static org.junit.jupiter.api.Assertions.*;

import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.jupiter.api.Test;

import junit.framework.Assert;

public class TestNotes {
	
	@Before == Before Each
	public static void setUp() throws Exception{
		// one-time initialisation code
	}
	
	
	@BeforeClass == BeforeAll in Junit 5
	public static void setUpClass() {
		//initialise variables
		
	}
	
	@Test (timeout = 10 //it is in miliseconds) // notation for junit 4
	@Timeout (10)
	void test() {
		//no parameters
		//no results
		//use assertion methods
		// assert("failure message", expected value, method, tolerance)
		// assert
		fail("Not yet implemented");
	}
	
	@Test (expected =IllegalArgumentException.class)
	
	@AfterClass
	public static void tearDownClass() throws Exception{
		//used to release files or other resources
	}
	
	@Test
	public void testException() {
		try {
			exceptionCausingMethod();
			
			// exceptio was not thrown.
			fail ("Exception should have occurred");
		} catch (ExpectedTypeofException exc) {
			String expected = "error message";
			String actual exc.getMessage();
			Assert.assertEquals(expected, actual);
			//comparing the message received by exception with message thrown by 
		}
	}
}
*/